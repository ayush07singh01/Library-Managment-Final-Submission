<dependency>
    <groupId>javax.servlet</groupId>
    <artifactId>jstl</artifactId>
    <version>1.2</version>
</dependency>

//Use JSTL and EL for dynamic content
<c:if test="${sessionScope.user != null}">
    <h3>Welcome, ${sessionScope.user}!</h3>
</c:if>
